/**
 * Replace this with a detailed description of what your custom JS snippet does.
 * @param {object} - mablInputs Object containing input
 *                   variables (mablInputs.variables.user)
 * @param {function} callback - The callback function
 */
function mablJavaScriptStep(mablInputs, callback) {

  // enter code here, include comments as neccesary. For example:

  // assigning a string value to result.
  let result = 'sample';
    
  // calling callback(result) assigns 'result' to a variable within the mabl trainer.
  callback(result);
}
